import {ServerExpress} from "./app"; //importamos la clase server
async function main(){ //funcion principal
    const app:ServerExpress = new  ServerExpress();//instanciamos la clase server
}
main(); //ejecutamos la funcion
